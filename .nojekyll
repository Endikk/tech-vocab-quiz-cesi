# This file tells GitHub Pages not to use Jekyll processing
# This is important for projects with files that start with underscores
# or have folders like node_modules that Jekyll would ignore by default
